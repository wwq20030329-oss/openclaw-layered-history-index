{
  "name": "layered-history-index",
  "version": "0.1.4",
  "private": true,
  "description": "Layered conversation memory plugin for OpenClaw",
  "type": "commonjs",
  "openclaw": {
    "extensions": ["./index.cjs"]
  },
  "scripts": {
    "test": "node --test",
    "analyze:trace": "node scripts/analyze-route-trace.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
